<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.enumflags">

    <uses-sdk android:minSdkVersion="24" />

    <!--
      Exercises enum/flag attributes the renderer resolves via ManifestEnumFlagNames:
      protectionLevel, launchMode, configChanges, screenOrientation, windowSoftInputMode.
    -->
    <permission
        android:name="com.example.enumflags.DANGEROUS_PERM"
        android:protectionLevel="signature|privileged" />

    <application android:label="EnumFlags Fixture">
        <activity
            android:name="com.example.enumflags.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:configChanges="mcc|mnc|locale|orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden"
            android:windowSoftInputMode="stateHidden|adjustResize" />
    </application>

</manifest>
